class Item {
  constructor(id, categoryIds, title, price, imageUrl) {
    this.id = id;
    this.categoryIds = categoryIds;
    this.title = title;
    this.price = price;
    this.imageUrl = imageUrl;
  }
}

export default Item;
